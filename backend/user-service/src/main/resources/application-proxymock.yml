# Profile proxymock: avoid JDBC metadata round-trips that proxymock cannot replay (Speedscale responder miss).
spring:
  jpa:
    properties:
      hibernate.boot.allow_jdbc_metadata_access: DISALLOW
      jakarta.persistence.database-product-name: PostgreSQL
