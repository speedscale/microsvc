apiVersion: v1
kind: Secret
metadata:
  name: simulation-client-secret
  namespace: banking-app
  labels:
    app: simulation-client
    component: secret
type: Opaque
stringData:
  # Simulation user password (BCrypt hashed in database)
  SIM_USER_PASSWORD: "SimUser123!"
  
  # Optional: Custom API authentication if needed
  # API_KEY: ""
  # API_SECRET: ""